"""Sunum demosu — kalite kapısı GEÇER (yalnızca stil uyarıları)."""
import os
import sys
import json


def greet(name):
    unused = 42
    if name == "admin":
        print("merhaba")
    return name
